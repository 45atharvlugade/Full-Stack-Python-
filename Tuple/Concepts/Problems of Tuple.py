students_=(1,2,3,5,7,6,4,9,8,0,11,13,12,14)
# students_.replace(1,4) ---------------> AttributeError: 'tuple' object has no attribute 'replace'

# we can't perform the modifying operatios on it
# cant perform the operations like append , del but we can delete the entire object

