# there is no symbolic notation but we can type cast it

set_=set({1,2,3,45,6})
frozenset_=frozenset(set_)
print(frozenset_,type(frozenset_))

# as it is immutable so modifications type function are not possible on this

